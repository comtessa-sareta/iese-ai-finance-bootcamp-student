"""Shared toolkit for the IESE AI-Augmented Productivity for Finance bootcamp.

Modules
-------
edgar : SEC EDGAR client — company lookup, filings metadata, XBRL financial facts.
llm   : Claude API helpers — plain asks, schema-validated JSON, tool-forced output.

Scripts in the session folders import this package by adding the repo root to
sys.path (see any demo script for the two-line pattern).
"""
